const Header = () => {
    return (
        <header>
            
        </header>
    );
}

const Footer = () => {
    return (
        <footer>
        </footer>
    );
}

export { Header, Footer };